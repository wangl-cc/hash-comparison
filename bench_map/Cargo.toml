[package]
name = "bench_map"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[[bench]]
harness = false
name = "map_comparison"

[dev-dependencies]
criterion = { workspace = true }
indexmap = { workspace = true }
rand = { workspace = true }
